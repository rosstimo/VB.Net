
Imports CoolLibrary

Module MyAwesomeApp

    Sub Main()
        Dim temp As String
        Dim menu As String

        'menu = "1. Delete" & vbNewLine _
        '     & "2. Save" & vbNewLine _
        '     & "3. Rename" & vbNewLine

        'temp = Prompt(menu)

        Prompt()

        'Console.WriteLine(temp)
        Console.Read()
    End Sub

End Module
